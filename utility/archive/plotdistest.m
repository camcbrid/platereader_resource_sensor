function plotdistest(cellstruct)

celltypes = fieldnames(cellstruct);

for ii = 1:length(celltypes)
    if isstruct(cellstruct.(celltypes{ii}))
        
        
    else
        
    end
end